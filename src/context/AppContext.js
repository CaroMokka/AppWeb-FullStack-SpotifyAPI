import { createContext } from "react";

export const AppContext = createContext();


export const ContextWrapper = () => {
    return (
        <AppContext.Provider>
            
        </AppContext.Provider>
    )




}